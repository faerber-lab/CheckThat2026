# GRITLM Fine-tuning Requirements
# For training GritLM models with LoRA on CheckThat dataset

# Core Deep Learning
torch>=2.0.0
numpy>=1.24.0

# Hugging Face Libraries
# Pinned to match gritlm library's hard requirements
transformers==4.37.2
datasets==2.16.1
accelerate==0.26.1
peft==0.10.0

# Data Processing
pandas>=2.0.0
tqdm>=4.65.0

# Training & Evaluation
mteb==1.4.0
wandb>=0.15.0

# Optional: For distributed training (if needed)
# deepspeed>=0.10.0
